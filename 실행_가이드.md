# 📊 연크크 ETF 대시보드 — 실행 가이드 (터미널 처음이어도 OK)

> 아래를 **순서대로** 따라 하세요. 명령어는 **복사해서 붙여넣기**만 하면 됩니다.
> (Mac / Windows 각자 본인 것만 보세요)

---

## 0단계. 파일 준비
- 받은 압축파일(zip)을 **압축 해제**하고, 그 폴더를 **바탕화면**처럼 찾기 쉬운 곳에 둡니다.

---

## 1단계. Python 설치 (최초 1회만)

1. https://www.python.org/downloads 접속 → 노란 **"Download Python"** 버튼 클릭 → 설치 파일 실행
2. ⚠️ **Windows 사용자**: 설치 첫 화면 맨 아래 **"Add Python to PATH"** 체크박스를 **반드시 체크**한 뒤 설치
3. 설치가 끝나면 컴퓨터를 한 번 **재시작**하면 더 안전합니다.

---

## 2단계. 터미널(명령창) 열기

### 🍎 Mac
- `command(⌘) + 스페이스바` → **"터미널"** 입력 → 엔터

### 🪟 Windows
- 키보드 **윈도우 키** → **"cmd"** 입력 → **명령 프롬프트** 클릭

검은(또는 흰) 창이 하나 뜨면 성공입니다.

---

## 3단계. 대시보드 폴더로 이동 (경로 안 쳐도 됨!)

1. 터미널 창에 먼저 이 세 글자를 칩니다 (cd 뒤에 **띄어쓰기 한 칸**):
   ```
   cd 
   ```
2. 그 상태에서 **1단계의 압축 푼 폴더를 마우스로 드래그해서 터미널 창에 떨어뜨립니다.**
   → 폴더 경로가 자동으로 입력됩니다.
3. **엔터**를 누릅니다.

---

## 4단계. 필요한 프로그램 설치 (최초 1회, 1~3분 소요)

아래를 복사해서 터미널에 붙여넣고 엔터:

### 🍎 Mac
```
python3 -m pip install -r requirements.txt
```

### 🪟 Windows
```
python -m pip install -r requirements.txt
```

> 글자가 와르르 지나가면 정상입니다. 멈추고 다시 입력 줄이 나오면 완료.

---

## 5단계. 대시보드 실행 🚀

### 🍎 Mac
```
python3 -m streamlit run dashboard.py
```

### 🪟 Windows
```
python -m streamlit run dashboard.py
```

- 잠시 후 **브라우저가 자동으로 열리며** 대시보드가 나타납니다.
- 자동으로 안 열리면, 브라우저 주소창에 직접 입력: **http://localhost:8501**

---

## ✅ 두 번째부터는?
- 1~2단계(설치)는 건너뛰고, **2 → 3 → 5단계**만 하면 됩니다.
  (터미널 열기 → 폴더로 이동 → 실행)

## ⏹️ 끄는 방법
- 대시보드를 그만 보려면, 터미널 창에서 `Ctrl + C`를 누르거나 창을 닫으면 됩니다.

---

## ❓ 잘 안 될 때
| 증상 | 해결 |
|------|------|
| `python3/python 명령을 찾을 수 없음` | 1단계 Python 설치 다시 (Windows는 "Add to PATH" 체크!) |
| `pip ... 오류` | 인터넷 연결 확인 후 4단계 재실행 |
| 브라우저가 안 열림 | 주소창에 `http://localhost:8501` 직접 입력 |
| 그 외 | 화면을 캡처해서 팀에 공유 |
